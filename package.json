{
  "name": "raio-x-checkout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Raio-X do Checkout — motor de auditoria (Fase 1: CLI)",
  "engines": { "node": ">=20.11" },
  "scripts": {
    "preflight": "tsx src/cli.ts preflight",
    "audit": "tsx src/cli.ts audit",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "1.56.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
